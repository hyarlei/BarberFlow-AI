# Test Environment Variables
NODE_ENV=test

# Database (usar banco separado para testes)
DATABASE_URL="postgresql://barberflow_user:barberflow_pass@localhost:5432/barberflow_test"

# JWT
JWT_SECRET="test-jwt-secret-for-e2e-tests"
JWT_EXPIRES_IN="7d"

# API
API_PORT=3001
API_PREFIX="api/v1"

# Bcrypt (menor para testes serem mais rápidos)
BCRYPT_ROUNDS=4

# Throttle (mais permissivo para testes)
THROTTLE_TTL=60
THROTTLE_LIMIT=1000

# Redis (usar database separado para testes)
REDIS_URL="redis://localhost:6379/1"
